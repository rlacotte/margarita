<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company %>
    <%= form.text_field :company, id: :company_company %>
  </div>

  <div class="field">
    <%= form.label :siret %>
    <%= form.text_field :siret, id: :company_siret %>
  </div>

  <div class="field">
    <%= form.label :step %>
    <%= form.select :step_id, Config::Step.all.collect {|s| [ s.step_name, s.id ] }, id: :company_step_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
