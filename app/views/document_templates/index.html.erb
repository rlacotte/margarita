<p id="notice"><%= notice %></p>

<h1>Document Templates</h1>

<table>
  <thead>
    <tr>
      <th>Document name</th>
      <th>Document type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @document_templates.each do |document_template| %>
      <tr>
        <td><%= document_template.document_name %></td>
        <td><%= document_template.document_type %></td>
        <td><%= link_to 'Show', document_template %></td>
        <td><%= link_to 'Edit', edit_document_template_path(document_template) %></td>
        <td><%= link_to 'Destroy', document_template, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Document Template', new_document_template_path %>
