<%= form_with(model: document_template, local: true) do |form| %>
  <% if document_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document_template.errors.count, "error") %> prohibited this document_template from being saved:</h2>

      <ul>
      <% document_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :document_name %>
    <%= form.text_field :document_name, id: :document_template_document_name %>
  </div>

  <div class="field">
    <%= form.label :document_type %>
    <%= form.text_field :document_type, id: :document_template_document_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
